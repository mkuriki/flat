<p id = notice></p>
<div class = container>
    <div class = row>
        <div class = "col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
            <h1>投稿編集</h1>
              <% if @post.errors.any? %>
                  <%= @post.errors.count %>errors prohibited this obj from being saved: 
                  <ul>
                  <% @post.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                  <% end %>
                  </ul>
              <% end %>             
              <%= form_with model: @post,url: admin_post_path do |f| %>
              <div class="form-group">
              <label for="post_title">タイトル</label>
              <%= f.text_field :title, autofocus: true, placeholder: @post.title, class: "form-control post_title" %>
              </div>
              <div class = "form-group">
              <label for="post_opinion">詳細</label>
              <%= f.text_area :body, autofocus: true, class: "form-control post_body" %>
              </div>
              <div class = "form-group">
              <%= f.submit "Update post", class: "btn btn-success"  %>
              </div>
              <%= link_to "戻る", admin_post_path(@post) %>
              <%= "|" %>
              <%= link_to "投稿一覧へ", admin_posts_path %>
            <% end %>
        </div>
    </div>
</d